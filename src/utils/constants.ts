export const INVALID_UNKNOWN_NAMES = ['INVALID', 'UNKNOWN'] as const;

export const MS_PER_MINUTE = 60 * 1000;
export const MS_PER_HOUR = 60 * MS_PER_MINUTE;
export const MS_PER_DAY = 24 * MS_PER_HOUR;
